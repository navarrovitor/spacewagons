<% if user_signed_in? && current_user.progress == 0%>

  <%= render 'shared/shipcreate' %>

<% elsif user_signed_in? && current_user.progress == 1 %>

  <%= render 'shared/partpick' %>

<% elsif user_signed_in? && current_user.progress == 2 %>

  <%= render 'shared/signed' %>

<% else %>

  <%= render 'shared/landing_page' %>

<% end %>
